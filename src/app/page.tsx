"use client"
export default function Home() {

  /* const user = JSON.parse(localStorage.getItem("UserInfo") || "");
  console.log(user) */

  return (
    <>
      <h1>Homepage</h1>
      {/* <h1>{user?.username}</h1> */}
    </>
  );
}
